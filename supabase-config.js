/* DRAPE — Supabase public config.
   Fill these two PUBLIC values after creating a free Supabase project:
   Supabase dashboard → Project Settings → API →
     • Project URL      → url
     • anon public key  → anonKey   (this key is meant to be public; safe to commit)
   Leaving the YOUR- placeholders makes the app fall back to open (no-account) mode. */
window.DRAPE_SUPABASE = {
  url: 'https://pbzjzdlobbfoprelnaff.supabase.co',
  anonKey: 'sb_publishable_OzFarh6hRPmU7CmGG2onxQ_h_AcKA2U'
};
